# Руководство по контрибуции

Спасибо за интерес к проекту go-cli! Мы рады любому вкладу в развитие проекта.

## 📋 Процесс контрибуции

### 1. Подготовка окружения

```bash
# Форкните репозиторий на GitHub
# Клонируйте ваш форк
git clone https://github.com/YOUR_USERNAME/software-design-mhs-itmo.git
cd software-design-mhs-itmo

# Добавьте upstream репозиторий
git remote add upstream https://github.com/ggerlakh/software-design-mhs-itmo.git

# Установите зависимости (если потребуется)
go mod download

# Установите golangci-lint для проверки кода
brew install golangci-lint  # для macOS
# или
go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
```

### 2. Создание ветки

```bash
# Обновите main ветку
git checkout main
git pull upstream main

# Создайте ветку для вашей работы
git checkout -b feature/your-feature-name
# или
git checkout -b fix/issue-number-description
```

### 3. Разработка

#### Соглашения по коду

- Следуйте [Effective Go](https://golang.org/doc/effective_go)
- Используйте `gofmt` для форматирования кода
- Добавляйте комментарии в формате GoDoc для всех публичных функций, типов и методов
- Имена переменных должны быть понятными и соответствовать контексту
- Избегайте магических чисел, используйте константы

#### Написание тестов

- Каждая новая функция должна иметь тесты
- Тесты должны покрывать как позитивные, так и негативные сценарии
- Используйте table-driven тесты, где это уместно
- Целевое покрытие: не менее 50%

Пример теста:

```go
func TestYourFunction(t *testing.T) {
    tests := []struct {
        name     string
        input    string
        expected string
        wantErr  bool
    }{
        {
            name:     "valid input",
            input:    "test",
            expected: "test",
            wantErr:  false,
        },
        // ... другие тест-кейсы
    }
    
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            result, err := YourFunction(tt.input)
            if (err != nil) != tt.wantErr {
                t.Errorf("YourFunction() error = %v, wantErr %v", err, tt.wantErr)
                return
            }
            if result != tt.expected {
                t.Errorf("YourFunction() = %v, want %v", result, tt.expected)
            }
        })
    }
}
```

### 4. Проверка кода

Перед коммитом убедитесь, что:

```bash
# Код форматирован
gofmt -w .

# Все тесты проходят
go test ./... -v

# Покрытие достаточное
go test ./... -cover

# Линтер не выдает ошибок
golangci-lint run

# Собирается без ошибок
go build -o go-cli ./cmd/go-cli
```

### 5. Отправка Pull Request

```bash
# Запуште вашу ветку
git push origin feature/your-feature-name
```

На GitHub:
1. Откройте Pull Request из вашей ветки в `main` ветку upstream
2. Заполните шаблон PR (если есть)
3. Опишите изменения:
   - Что было сделано
   - Зачем (ссылка на issue, если есть)
   - Как проверить
   - Скриншоты/gif (если применимо)

### 6. Ревью

- Отвечайте на комментарии ревьюеров
- Вносите запрошенные изменения
- После одобрения ваш PR будет смержен

## 🐛 Сообщение об ошибках

При создании issue с багом включите:

1. **Описание**: что произошло
2. **Ожидаемое поведение**: что должно было произойти
3. **Шаги для воспроизведения**:
   ```
   1. Запустить go-cli
   2. Ввести команду "..."
   3. Наблюдать ошибку
   ```
4. **Окружение**:
   - ОС и версия (macOS 14.0, Ubuntu 22.04 и т.п.)
   - Версия Go (`go version`)
   - Версия go-cli (commit hash или tag)
5. **Дополнительный контекст**: логи, скриншоты и т.п.

## 💡 Предложение новых функций

При создании issue с feature request включите:

1. **Описание функции**: что вы хотите добавить
2. **Мотивация**: зачем это нужно
3. **Примеры использования**: как это будет использоваться
4. **Альтернативы**: рассматривали ли другие подходы
5. **Дополнительный контекст**: примеры из других проектов и т.п.

## 📝 Улучшение документации

Документация так же важна, как и код! Вы можете помочь:

- Исправить опечатки
- Улучшить объяснения
- Добавить примеры
- Перевести на другие языки

Для документации действуют те же правила, что и для кода.

## ❓ Вопросы

Если у вас есть вопросы:

1. Проверьте [README.md](README.md) и документацию
2. Поищите в [Issues](https://github.com/ggerlakh/software-design-mhs-itmo/issues)
3. Создайте новый issue с тегом `question`

## 📜 Лицензия

Внося вклад в проект, вы соглашаетесь, что ваш код будет лицензирован под [MIT License](LICENSE.TXT).

---

Спасибо за ваш вклад! 🎉

